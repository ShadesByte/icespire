{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"],
  "compilerOptions": {
    // src/lib/og.ts runs in Node at build time (fs, path, process, Buffer).
    "types": ["node"]
  }
}
